<form method="post" action="{{ path('book_club_month_delete', {'id': book_club_month.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ book_club_month.id) }}">
    <button class="btn">Delete</button>
</form>
